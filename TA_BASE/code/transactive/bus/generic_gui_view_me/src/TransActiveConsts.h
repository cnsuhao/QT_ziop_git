#ifndef TRANSACTIVE_CONSTS_H__
#define TRANSACTIVE_CONSTS_H__

    // Get a unique user defined message id for set window position message.
    // Send WM_SET_WINDOW_POSITION message with TA_Rect* as first argument (NULL if not POSITION)
    // and the  EWindowPosition as second argument.
    // static UINT WM_SET_WINDOW_POSITION = RegisterWindowMessageA("SetWindowPosition");

    // #define APPCOMMAND 0xFFF0

#endif
